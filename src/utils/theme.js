export const lightTheme = {
    colors: {
      primary: '#22C55E',      // Verde principal
      secondary: '#2ecc71',    // Verde secundário
      background: '#f5f5f5',   // Fundo principal (claro)
      surface: '#DFF0DB',      // Superfície (ex.: cartões ou contêineres)
      textPrimary: '#000000',  // Texto principal (preto)
      textSecondary: '#666666',// Texto secundário
      textMuted: '#7f8c8d',    // Texto menos destacado
      white: '#FFFFFF',        // Branco
      black: '#000000',        // Preto
    },
  };
  
  export const darkTheme = {
    colors: {
      primary: '#22C55E',      // Verde principal
      secondary: '#2ecc71',    // Verde secundário
      background: '#000000',   // Fundo principal (preto)
      surface: '#121212',      // Superfície (tons mais escuros para cartões, etc.)
      textPrimary: '#FFFFFF',  // Texto principal (branco)
      textSecondary: '#CCCCCC',// Texto secundário (cinza claro)
      textMuted: '#7f8c8d',    // Texto menos destacado (cinza médio)
      white: '#FFFFFF',        // Branco (para contraste com fundo escuro)
      black: '#000000',        // Preto (preservado para referências)
    },
  };
  